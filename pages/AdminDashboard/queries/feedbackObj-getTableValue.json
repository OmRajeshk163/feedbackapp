{
  "pluginType": "JS",
  "pluginId": "js-plugin",
  "unpublishedAction": {
    "name": "getTableValue",
    "fullyQualifiedName": "feedbackObj.getTableValue",
    "datasource": {
      "name": "UNUSED_DATASOURCE",
      "pluginId": "js-plugin",
      "messages": [],
      "isAutoGenerated": false,
      "deleted": false,
      "policies": [],
      "userPermissions": []
    },
    "pageId": "AdminDashboard",
    "collectionId": "AdminDashboard_feedbackObj",
    "actionConfiguration": {
      "timeoutInMillisecond": 10000,
      "paginationType": "NONE",
      "encodeParamsToggle": true,
      "body": "() => {\n  const feedbackList = getFeedback.data;\n  console.log(\"gfhfrscondition\", cycleSelect.selectedOptionValue, fromEmpSelect.selectedOptionValue.length !== 0 && toEmpSelect.selectedOptionValue.length !== 0 && cycleSelect.selectedOptionValue.length !== 0 && kraSelect.selectedOptionValue.length === 0);\n  if (fromEmpSelect.selectedOptionValue.length === 0 && toEmpSelect.selectedOptionValue.length === 0 && cycleSelect.selectedOptionValue.length === 0 && kraSelect.selectedOptionValue.length === 0) {\n    return feedbackList.map(feed => ({\n      From_name: getEmployees.data.find(emp => feed.from_employee == emp.id).name,\n      To_name: getEmployees.data.find(emp => emp.id === feed.to_employee)?.name || \"N/A\",\n      Category: getKraCategories.data.find(kra => feed.category == kra.id).name,\n      Feedback_continue: feed.feedback_continue,\n      Feedback_can_do_more: feed.feedback_more_of,\n      Rating: feed.rating,\n      Feedback_type: feed.feedback_type,\n      Cycle: feed.cycle\n    })).filter(e => e);\n  } else if (fromEmpSelect.selectedOptionValue.length !== 0 && toEmpSelect.selectedOptionValue.length === 0 && cycleSelect.selectedOptionValue.length === 0 && kraSelect.selectedOptionValue.length === 0) {\n    return feedbackList.map(fdbk => {\n      if (fdbk.from_employee == fromEmpSelect.selectedOptionValue) {\n        console.log(\"tgrerdfeerer\", getEmployees.data.find(emp => fromEmpSelect.selectedOptionValue == emp.id)?.name);\n        return {\n          From_name: getEmployees.data.find(emp => fromEmpSelect.selectedOptionValue == emp.id)?.name,\n          To_name: getEmployees.data.find(emp => emp.id === fdbk.to_employee)?.name,\n          Category: getKraCategories.data.find(kra => fdbk.category == kra.id).name,\n          Feedback_continue: fdbk.feedback_continue,\n          Feedback_can_do_more: fdbk.feedback_more_of,\n          Rating: fdbk.rating,\n          Feedback_type: fdbk.feedback_type,\n          Cycle: fdbk.cycle\n        };\n      }\n    }).filter(e => e);\n  } else if (fromEmpSelect.selectedOptionValue.length !== 0 && toEmpSelect.selectedOptionValue.length !== 0 && cycleSelect.selectedOptionValue.length === 0 && kraSelect.selectedOptionValue.length === 0) {\n    return feedbackList.map(fdbk => {\n      if (fdbk.from_employee == fromEmpSelect.selectedOptionValue && fdbk.to_employee == toEmpSelect.selectedOptionValue) return {\n        From_name: getEmployees.data.find(emp => fdbk.from_employee == emp.id)?.name,\n        To_name: getEmployees.data.find(emp => emp.id === fdbk.to_employee)?.name,\n        Category: getKraCategories.data.find(kra => fdbk.category == kra.id).name,\n        Feedback_continue: fdbk.feedback_continue,\n        Feedback_can_do_more: fdbk.feedback_more_of,\n        Rating: fdbk.rating,\n        Feedback_type: fdbk.feedback_type,\n        Cycle: fdbk.cycle\n      };\n    }).filter(e => e);\n  } else if (fromEmpSelect.selectedOptionValue.length !== 0 && toEmpSelect.selectedOptionValue.length !== 0 && cycleSelect.selectedOptionValue.length !== 0 && kraSelect.selectedOptionValue.length === 0) {\n    return feedbackList.map(fdbk => {\n      if (fdbk.from_employee === fromEmpSelect.selectedOptionValue && fdbk.to_employee === toEmpSelect.selectedOptionValue && fdbk.cycle === cycleSelect.selectedOptionLabel) return {\n        From_name: getEmployees.data.find(emp => fdbk.from_employee == emp.id)?.name,\n        To_name: getEmployees.data.find(emp => emp.id === fdbk.to_employee)?.name,\n        Category: getKraCategories.data.find(kra => fdbk.category == kra.id).name,\n        Feedback_continue: fdbk.feedback_continue,\n        Feedback_can_do_more: fdbk.feedback_more_of,\n        Rating: fdbk.rating,\n        Feedback_type: fdbk.feedback_type,\n        Cycle: fdbk.cycle\n      };\n    }).filter(e => e);\n  } else if (fromEmpSelect.selectedOptionValue.length !== 0 && toEmpSelect.selectedOptionValue.length !== 0 && kraSelect.selectedOptionValue.length !== 0 && cycleSelect.selectedOptionValue.length === 0) {\n    return feedbackList.map(fdbk => {\n      if (fdbk.from_employee == fromEmpSelect.selectedOptionValue && fdbk.to_employee == toEmpSelect.selectedOptionValue && fdbk.category == kraSelect.selectedOptionValue) return {\n        From_name: getEmployees.data.find(emp => fdbk.from_employee == emp.id)?.name,\n        To_name: getEmployees.data.find(emp => emp.id === fdbk.to_employee)?.name,\n        Category: getKraCategories.data.find(kra => fdbk.category == kra.id).name,\n        Feedback_continue: fdbk.feedback_continue,\n        Feedback_can_do_more: fdbk.feedback_more_of,\n        Rating: fdbk.rating,\n        Feedback_type: fdbk.feedback_type,\n        Cycle: fdbk.cycle\n      };\n    }).filter(e => e);\n  } else {\n    return feedbackList.map(fdbk => {\n      if (fdbk.from_employee == fromEmpSelect.selectedOptionValue && fdbk.to_employee == toEmpSelect.selectedOptionValue && fdbk.category == kraSelect.selectedOptionValue && fdbk.cycle == cycleSelect.selectedOptionLabel) return {\n        From_name: getEmployees.data.find(emp => fdbk.from_employee == emp.id)?.name,\n        To_name: getEmployees.data.find(emp => emp.id === fdbk.to_employee)?.name,\n        Category: getKraCategories.data.find(kra => fdbk.category == kra.id).name,\n        Feedback_continue: fdbk.feedback_continue,\n        Feedback_can_do_more: fdbk.feedback_more_of,\n        Rating: fdbk.rating,\n        Feedback_type: fdbk.feedback_type,\n        Cycle: fdbk.cycle\n      };\n    }).filter(e => e);\n  }\n}",
      "selfReferencingDataPaths": [],
      "jsArguments": [],
      "isAsync": false
    },
    "executeOnLoad": false,
    "dynamicBindingPathList": [
      {
        "key": "body"
      }
    ],
    "isValid": true,
    "invalids": [],
    "messages": [],
    "jsonPathKeys": [
      "() => {\n  const feedbackList = getFeedback.data;\n  console.log(\"gfhfrscondition\", cycleSelect.selectedOptionValue, fromEmpSelect.selectedOptionValue.length !== 0 && toEmpSelect.selectedOptionValue.length !== 0 && cycleSelect.selectedOptionValue.length !== 0 && kraSelect.selectedOptionValue.length === 0);\n  if (fromEmpSelect.selectedOptionValue.length === 0 && toEmpSelect.selectedOptionValue.length === 0 && cycleSelect.selectedOptionValue.length === 0 && kraSelect.selectedOptionValue.length === 0) {\n    return feedbackList.map(feed => ({\n      From_name: getEmployees.data.find(emp => feed.from_employee == emp.id).name,\n      To_name: getEmployees.data.find(emp => emp.id === feed.to_employee)?.name || \"N/A\",\n      Category: getKraCategories.data.find(kra => feed.category == kra.id).name,\n      Feedback_continue: feed.feedback_continue,\n      Feedback_can_do_more: feed.feedback_more_of,\n      Rating: feed.rating,\n      Feedback_type: feed.feedback_type,\n      Cycle: feed.cycle\n    })).filter(e => e);\n  } else if (fromEmpSelect.selectedOptionValue.length !== 0 && toEmpSelect.selectedOptionValue.length === 0 && cycleSelect.selectedOptionValue.length === 0 && kraSelect.selectedOptionValue.length === 0) {\n    return feedbackList.map(fdbk => {\n      if (fdbk.from_employee == fromEmpSelect.selectedOptionValue) {\n        console.log(\"tgrerdfeerer\", getEmployees.data.find(emp => fromEmpSelect.selectedOptionValue == emp.id)?.name);\n        return {\n          From_name: getEmployees.data.find(emp => fromEmpSelect.selectedOptionValue == emp.id)?.name,\n          To_name: getEmployees.data.find(emp => emp.id === fdbk.to_employee)?.name,\n          Category: getKraCategories.data.find(kra => fdbk.category == kra.id).name,\n          Feedback_continue: fdbk.feedback_continue,\n          Feedback_can_do_more: fdbk.feedback_more_of,\n          Rating: fdbk.rating,\n          Feedback_type: fdbk.feedback_type,\n          Cycle: fdbk.cycle\n        };\n      }\n    }).filter(e => e);\n  } else if (fromEmpSelect.selectedOptionValue.length !== 0 && toEmpSelect.selectedOptionValue.length !== 0 && cycleSelect.selectedOptionValue.length === 0 && kraSelect.selectedOptionValue.length === 0) {\n    return feedbackList.map(fdbk => {\n      if (fdbk.from_employee == fromEmpSelect.selectedOptionValue && fdbk.to_employee == toEmpSelect.selectedOptionValue) return {\n        From_name: getEmployees.data.find(emp => fdbk.from_employee == emp.id)?.name,\n        To_name: getEmployees.data.find(emp => emp.id === fdbk.to_employee)?.name,\n        Category: getKraCategories.data.find(kra => fdbk.category == kra.id).name,\n        Feedback_continue: fdbk.feedback_continue,\n        Feedback_can_do_more: fdbk.feedback_more_of,\n        Rating: fdbk.rating,\n        Feedback_type: fdbk.feedback_type,\n        Cycle: fdbk.cycle\n      };\n    }).filter(e => e);\n  } else if (fromEmpSelect.selectedOptionValue.length !== 0 && toEmpSelect.selectedOptionValue.length !== 0 && cycleSelect.selectedOptionValue.length !== 0 && kraSelect.selectedOptionValue.length === 0) {\n    return feedbackList.map(fdbk => {\n      if (fdbk.from_employee === fromEmpSelect.selectedOptionValue && fdbk.to_employee === toEmpSelect.selectedOptionValue && fdbk.cycle === cycleSelect.selectedOptionLabel) return {\n        From_name: getEmployees.data.find(emp => fdbk.from_employee == emp.id)?.name,\n        To_name: getEmployees.data.find(emp => emp.id === fdbk.to_employee)?.name,\n        Category: getKraCategories.data.find(kra => fdbk.category == kra.id).name,\n        Feedback_continue: fdbk.feedback_continue,\n        Feedback_can_do_more: fdbk.feedback_more_of,\n        Rating: fdbk.rating,\n        Feedback_type: fdbk.feedback_type,\n        Cycle: fdbk.cycle\n      };\n    }).filter(e => e);\n  } else if (fromEmpSelect.selectedOptionValue.length !== 0 && toEmpSelect.selectedOptionValue.length !== 0 && kraSelect.selectedOptionValue.length !== 0 && cycleSelect.selectedOptionValue.length === 0) {\n    return feedbackList.map(fdbk => {\n      if (fdbk.from_employee == fromEmpSelect.selectedOptionValue && fdbk.to_employee == toEmpSelect.selectedOptionValue && fdbk.category == kraSelect.selectedOptionValue) return {\n        From_name: getEmployees.data.find(emp => fdbk.from_employee == emp.id)?.name,\n        To_name: getEmployees.data.find(emp => emp.id === fdbk.to_employee)?.name,\n        Category: getKraCategories.data.find(kra => fdbk.category == kra.id).name,\n        Feedback_continue: fdbk.feedback_continue,\n        Feedback_can_do_more: fdbk.feedback_more_of,\n        Rating: fdbk.rating,\n        Feedback_type: fdbk.feedback_type,\n        Cycle: fdbk.cycle\n      };\n    }).filter(e => e);\n  } else {\n    return feedbackList.map(fdbk => {\n      if (fdbk.from_employee == fromEmpSelect.selectedOptionValue && fdbk.to_employee == toEmpSelect.selectedOptionValue && fdbk.category == kraSelect.selectedOptionValue && fdbk.cycle == cycleSelect.selectedOptionLabel) return {\n        From_name: getEmployees.data.find(emp => fdbk.from_employee == emp.id)?.name,\n        To_name: getEmployees.data.find(emp => emp.id === fdbk.to_employee)?.name,\n        Category: getKraCategories.data.find(kra => fdbk.category == kra.id).name,\n        Feedback_continue: fdbk.feedback_continue,\n        Feedback_can_do_more: fdbk.feedback_more_of,\n        Rating: fdbk.rating,\n        Feedback_type: fdbk.feedback_type,\n        Cycle: fdbk.cycle\n      };\n    }).filter(e => e);\n  }\n}"
    ],
    "userSetOnLoad": false,
    "confirmBeforeExecute": false,
    "policies": [],
    "userPermissions": []
  },
  "id": "AdminDashboard_feedbackObj.getTableValue",
  "deleted": false,
  "gitSyncId": "6361ef20dfa75b056e2ed6b2_63844d29fa57b54aac1bdd21"
}